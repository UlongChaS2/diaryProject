import React from 'react';
import styled from 'styled-components/macro';

const Form: React.FC = () => {
  return (
    <Article>
      <div>안녕하세요</div>
    </Article>
  );
};

export default Form;

const Article = styled.article``;
